#!/bin/sh
set -e
if [ "$1" = configure ]; then
    systemctl daemon-reload || true
    systemctl enable --now gf63-lid.service || echo 'gf63-lid: service activation failed; check systemctl status gf63-lid' >&2
fi
exit 0
